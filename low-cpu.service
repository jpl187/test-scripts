[Unit]
Description=Run clamscan when CPU usage is low for 2 hours, max once daily
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 /path/to/clamscan_low_cpu.py
Restart=on-failure
User=root
Group=root

[Install]
WantedBy=multi-user.target
